[project]
name = "hennongxi-contracts"
version = "0.1.0"
description = "Versioned API and event contracts for the Hennongxi agent system"
requires-python = "==3.12.*"
dependencies = ["fastapi==0.139.2", "pydantic==2.13.4"]

[build-system]
requires = ["uv-build==0.11.29"]
build-backend = "uv_build"
